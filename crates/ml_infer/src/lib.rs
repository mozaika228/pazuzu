// Minimal inference stub. Replace with ONNX/TinyML implementation later.

pub struct Model;

impl Model {
    pub fn load(_path: &str) -> Self {
        Model
    }

    pub fn predict(&self, _features: &[f32]) -> f32 {
        0.0
    }
}
